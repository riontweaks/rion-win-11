<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <!-- Page-scoped treatments keep the rest of the app's theme unchanged. -->
    <SolidColorBrush x:Key="GoodWash" Color="#294ADE9B"/>
    <SolidColorBrush x:Key="CautionWash" Color="#29FFC53D"/>
    <Style x:Key="TweakCaption" TargetType="TextBlock" BasedOn="{StaticResource Caption}">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style x:Key="TweakSurface" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Bg1}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Divider}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
    </Style>
    <Style x:Key="TweakCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Bg1}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Divider}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Style.Triggers>
            
        </Style.Triggers>
    </Style>
    <!-- Featured cards use the same restrained border as every other card; no glow treatment. -->
    <Style x:Key="TweakGlow" TargetType="Border">
        <Setter Property="Visibility" Value="Collapsed"/>
    </Style>
    <Style x:Key="TweakCardTitle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DisplayFont}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style x:Key="TweakCardDescription" TargetType="TextBlock" BasedOn="{StaticResource TweakCaption}">
        <Setter Property="Margin" Value="0,4,0,0"/>
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
    </Style>
    <Style x:Key="TweakMetaText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource TextTertiary}"/>
        <Setter Property="Margin" Value="0,0,14,0"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style x:Key="CardControls" TargetType="StackPanel">
        <Setter Property="Grid.Column" Value="1"/>
        <Setter Property="Grid.Row" Value="0"/>
        <Setter Property="Margin" Value="20,0,0,0"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="MaxWidth" Value="200"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsCompact, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                <Setter Property="Grid.Column" Value="0"/>
                <Setter Property="Grid.Row" Value="1"/>
                <Setter Property="Grid.ColumnSpan" Value="2"/>
                <Setter Property="Margin" Value="0,12,0,0"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="Orientation" Value="Horizontal"/>
                <Setter Property="MaxWidth" Value="{x:Static sys:Double.PositiveInfinity}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="CardPresetBlock" TargetType="StackPanel">
        <Setter Property="Margin" Value="0,9,0,0"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsCompact, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                <Setter Property="Margin" Value="14,0,0,0"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="TweakSectionHeader" TargetType="DockPanel">
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>
    <Style x:Key="TweakSectionTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="VerticalAlignment" Value="Bottom"/>
    </Style>
    <Style x:Key="TweakCautionSlab" TargetType="Border">
        <Setter Property="BorderBrush" Value="{StaticResource CautionCol}"/>
        <Setter Property="BorderThickness" Value="2,0,0,0"/>
        <Setter Property="Padding" Value="10,5,0,5"/>
        <Setter Property="Margin" Value="0,10,0,0"/>
    </Style>
    <Style x:Key="TweakSlabHeading" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource CautionCol}"/>
    </Style>
    <Style x:Key="TweakLedgerValue" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource AccentLight}"/>
    </Style>
    <Style x:Key="TweakLedgerPath" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource TextTertiary}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="0,2,0,0"/>
    </Style>
    <Style x:Key="TweakAction" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ControlBg}"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderCol}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="frame" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        
                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="frame" Property="Opacity" Value="0.8"/></Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="frame" Property="BorderBrush" Value="{StaticResource AccentLight}"/><Setter TargetName="frame" Property="BorderThickness" Value="2"/></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="TweakPrimaryAction" TargetType="Button" BasedOn="{StaticResource TweakAction}">
        <Setter Property="Background" Value="{StaticResource AccentLight}"/>
        <Setter Property="Foreground" Value="{StaticResource Bg0}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentLight}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="ContentTemplate"><Setter.Value><DataTemplate>
            <TextBlock Text="{Binding}" Foreground="{StaticResource Bg0}" FontSize="13" FontWeight="SemiBold"/>
        </DataTemplate></Setter.Value></Setter>
        <Style.Triggers><Trigger Property="IsKeyboardFocused" Value="True">
            <Setter Property="Background" Value="{StaticResource Text}"/>
        </Trigger></Style.Triggers>
    </Style>
    <Style x:Key="TweakSwitch" TargetType="ToggleButton">
        <Setter Property="Width" Value="56"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="focus" Background="Transparent" BorderBrush="Transparent" BorderThickness="2" CornerRadius="6">
                        <Grid Width="40" Height="22">
                            <Border x:Name="track" Background="{StaticResource SurfaceSunk}" BorderBrush="{StaticResource BorderStrong}" BorderThickness="1" CornerRadius="11"/>
                            <Ellipse x:Name="knob" Width="14" Height="14" Fill="{StaticResource TextDim}" HorizontalAlignment="Left" Margin="4,0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True"><Setter TargetName="track" Property="Background" Value="{StaticResource AccentLight}"/><Setter TargetName="knob" Property="Fill" Value="{StaticResource Bg0}"/><Setter TargetName="knob" Property="HorizontalAlignment" Value="Right"/></Trigger>
                        
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="focus" Property="BorderBrush" Value="{StaticResource AccentLight}"/></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="TweakDisclosure" TargetType="Expander">
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <StackPanel>
                        <ToggleButton x:Name="header" MinHeight="36" HorizontalContentAlignment="Left"
                                      AutomationProperties.Name="{Binding Path=(AutomationProperties.Name), RelativeSource={RelativeSource TemplatedParent}}"
                                      IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="focus" BorderBrush="Transparent" BorderThickness="1" CornerRadius="4" Background="Transparent" Padding="2,6">
                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="18"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Path x:Name="arrow" Data="M 0,0 L 4,4 L 0,8" Stroke="{StaticResource TextDim}" StrokeThickness="1.5" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True"><Setter TargetName="arrow" Property="Data" Value="M 0,0 L 4,4 L 8,0"/></Trigger>
                                        
                                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="focus" Property="BorderBrush" Value="{StaticResource AccentLight}"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <ContentPresenter Content="{TemplateBinding Header}"/>
                        </ToggleButton>
                        <ContentPresenter x:Name="body" Visibility="Collapsed" Margin="18,4,0,8"/>
                    </StackPanel>
                    <ControlTemplate.Triggers><Trigger Property="IsExpanded" Value="True"><Setter TargetName="body" Property="Visibility" Value="Visible"/></Trigger></ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="TweakFilters" TargetType="ListBox" BasedOn="{StaticResource Segmented}">
        <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBox"><WrapPanel IsItemsHost="True"/></ControlTemplate></Setter.Value></Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value><Style TargetType="ListBoxItem">
                <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="item" Padding="10,8" Margin="0,0,4,4" MinHeight="36" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="5">
                        <ContentPresenter TextElement.FontFamily="{StaticResource AppFont}" TextElement.FontSize="13"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True"><Setter TargetName="item" Property="Background" Value="{StaticResource AccentWashHi}"/><Setter Property="Foreground" Value="{StaticResource Text}"/></Trigger>
                        
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="item" Property="BorderBrush" Value="{StaticResource AccentLight}"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate></Setter.Value></Setter>
            </Style></Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CardSwitch" TargetType="ToggleButton" BasedOn="{StaticResource TweakSwitch}">
        <Setter Property="Margin" Value="0,9,0,0"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsCompact, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                <Setter Property="Margin" Value="14,0,0,0"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
