<UserControl x:Class="RadeonSoftwareSlimmer.Views.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:RadeonSoftwareSlimmer.ViewModels" FontFamily="{DynamicResource AppFont}" UseLayoutRounding="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                   Style="{StaticResource Caption}"
                   Text="{Binding Path=(vm:StaticViewModel.StatusMessage)}"/>
        <ProgressBar Grid.Column="1" Width="120" Height="4" VerticalAlignment="Center"
                     IsIndeterminate="{Binding Path=(vm:StaticViewModel.IsLoading)}"/>
    </Grid>
</UserControl>
